# x402-exec Showcase Client Environment Variables
# All variables must be prefixed with VITE_ for Vite to expose them to the client

# Facilitator URL
# Default: https://facilitator.x402x.dev
# For local development: http://localhost:3001
VITE_FACILITATOR_URL=https://facilitator.x402x.dev

# Server URL (for Server Mode examples)
# Leave empty to use relative paths (Vite proxy in dev, same-origin in production)
VITE_SERVER_URL=https://x402-exec-showcase-server-test.up.railway.app

# Your address
VITE_DEFAULT_RECIPIENT_ADDRESS=
